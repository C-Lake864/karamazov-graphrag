# 이 파일을 .env 로 복사한 뒤 값을 채우세요 (UTF-8 로 저장).
# .env 는 .gitignore 에 있어 저장소에 올라가지 않습니다.

# 기본 제공자 (config.json 의 "provider": "openai")
OPENAI_API_KEY=

# config.json 의 "provider" 를 "google" 로 바꿀 때만 필요
# GOOGLE_API_KEY=
