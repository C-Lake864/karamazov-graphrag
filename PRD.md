# PRD — 카라마조프가의 형제들 관계도 도우미 (스포 방지 GraphRAG 에이전트)

작성일: 2026-09-21

---

## 1. 한 줄 요약

《카라마조프가의 형제들》을 읽는 사람이, **자기가 읽은 지점까지의 내용만으로** 인물 관계와 별명을 물어볼 수 있는 한국어 챗봇.

---

## 2. 해결할 문제

- **처음 시작할 때 진입장벽이 높다.** 인물이 한꺼번에 쏟아지고 누가 누구인지 안 잡힌다.
- **읽는 중에 별명이 나와서 헷갈린다.** 러시아 소설 특유의 애칭·부칭(父稱)이 섞여 나오는데, 같은 사람인지 다른 사람인지 매번 막힌다.
- **기존 방법으로는 해결이 안 된다.** 위키백과나 일반 챗봇에 물으면 아직 안 읽은 부분까지 다 말해버려서, 궁금증을 푸는 대가로 스포를 당한다.

---

## 3. 사용자

- **주 사용자: 나 혼자.** 여러 명이 쓰지 않으므로 로그인·공유·계정 없음.
- **사용 상황:** 책을 읽다가 막혔을 때. "얘가 아까 그 사람 맞나?", "이 둘이 무슨 사이였지?" 싶을 때 브라우저를 열어 묻는다.

---

## 4. 핵심 기능

| 기능 | 설명 | 우선순위 |
|---|---|---|
| **읽은 지점 설정** | 1~96장 중 어디까지 읽었는지 지정. 한 번 정하면 계속 유지되고, 질문을 여러 번 해도 안 풀린다. 바꾸고 싶을 때만 바꾼다. | **필수** |
| **스포 차단** | 읽은 지점보다 뒤에서 나온 근거는 검색 단계에서 아예 제거. LLM에게 "말하지 마"라고 부탁하는 게 아니라 볼 수 없게 만든다. | **필수** |
| **별명·별칭 병합** | 드미트리 = 미챠 = 미첸카 처럼 같은 인물의 여러 호칭을 하나로 묶어서 알려준다. 단, 아직 안 읽은 지점에서 밝혀지는 호칭은 감춘다. | **필수** |
| **멀티홉 관계 답변** | 한 문서만 봐서는 안 나오는 질문(2홉 이상)에, 여러 장을 건너뛰며 찾아 답한다. | **필수** |
| **경로·근거 표시** | 답변과 함께 ①실제로 탄 관계 경로 ②근거 삼중항 ③출처(몇 편 몇 장)를 같이 보여준다. | **필수** |
| **한국어 문답** | 질문도 한국어, 답변도 한국어. 근거 인용문만 영어 원문. | **필수** |
| **모른다고 말하기** | 읽은 범위 안에 근거가 없으면 지어내지 않고 "아직 안 나온 내용"이라고 답한다. | **필수** |
| **관계도 그림** | 답변에 쓰인 부분 그래프를 그림으로 보여주기 | 나중에 |
| **인물 카드** | 인물 하나를 골라 프로필처럼 보기 | 나중에 |

---

## 5. 사용 흐름

1. 브라우저에서 화면을 연다.
2. **읽은 지점을 정한다.** 슬라이더를 밀거나(1~96장), 편/장 2단 선택으로 고른다. 아직 안 읽은 장은 **제목을 감추고 번호만** 보여준다 (장 제목 자체가 스포라서).
3. 질문을 한국어로 입력한다. — 예: "알료샤의 형이 사랑하는 여자와 다투는 사람은 누구야?"
4. 답변을 한국어로 받는다. 밑에 **탄 경로 · 근거 삼중항 · 출처 장**이 함께 나온다.
5. 읽은 지점은 그대로 유지된 채 3~4를 반복한다. 책을 더 읽으면 2로 돌아가 지점만 옮긴다.

---

## 6. 데이터

### 원본 문서 (`data/docs/`)
- **출처:** Project Gutenberg의 영어 번역본 (Constance Garnett 역, 1880년 원작 / 저작권 만료, 무료 공개).
- **쪼개는 단위:** **장(Chapter)**. 4부 → 12편 → 각 편의 장. 에필로그 포함 **약 96건** (정확한 수는 빌드할 때 세어서 확인).
- **왜 장 단위인가:** ①스포 차단을 장 단위로 정밀하게 하려고 ②과제 요건인 "문서 50건 이상"과 "개체가 문서 사이에 겹칠 것"을 자연스럽게 충족하려고.
- 각 문서에는 `편 번호 / 장 번호 / 통짜 번호(1~96) / 장 제목`이 붙는다. **통짜 번호가 스포 차단의 기준값**이다.

### 지식 그래프 (`output/graph.graphml`)
- 노드와 관계(삼중항). **모든 관계에 "최초로 드러난 장 번호"와 "근거 인용문"이 붙는다.** 이게 없으면 스포 차단이 불가능하므로 필수 속성.
- 별칭 사전(같은 인물의 여러 호칭)도 함께 저장.

### 실행 기록
- `output/runs.jsonl` — 질문마다 탄 경로와 근거
- `output/eval.json` — 평가 결과
- `data/goldenset.json` — 평가셋 10건 이상

### 저장 위치
**내 컴퓨터 안 파일로만.** 데이터베이스 없음, 서버 없음, 로그인 없음. 읽은 지점은 브라우저 세션에 들고 있는다.

---

## 7. 기술 & 환경

| 항목 | 정한 것 | 이유 |
|---|---|---|
| 실행 환경 | 브라우저 (내 컴퓨터에서 띄움) | 사용자가 브라우저로 지정 |
| 화면 | Streamlit 한 페이지 *(제가 정했습니다)* | 읽은 지점 + 질문 + 답변/경로/근거가 한 화면에 들어가고, 파이썬만으로 끝나서 제일 빠름 |
| LLM | OpenAI API | 사용자가 키를 이미 보유 |
| 에이전트 구조 | LangGraph | 과제 요구사항 (상태 흐름도 제출) |
| 그래프 저장 | NetworkX + graphml 파일 *(제가 정했습니다)* | 내 컴퓨터에만 두기로 했으므로 DB를 띄울 이유가 없음 |
| 키 관리 | `.env` (깃헙에 올리지 않음) | 과제 지시 |

---

## 8. 완성 기준

사용자 기준:
- [ ] 직접 궁금해서 던진 질문 **3개 이상에서 답이 만족스럽다** (사용자 본인 판단)
- [ ] 읽은 지점을 3편으로 두고 뒷부분을 물었을 때, **스포하지 않고 "아직 안 나왔다"고 답한다**
- [ ] 별명을 물었을 때 같은 인물의 호칭들을 묶어서 알려준다
- [ ] 질문을 10번 이어서 해도 읽은 지점이 안 풀린다

과제 기준:
- [ ] 문서 50건 이상 (장 단위 약 96건)
- [ ] 골든셋 10문항 이상 — 기대 정답 · 기대 경로 · 원문 근거 포함
- [ ] 홉 수별 basic RAG 대조표 + 경로 재현율 + 실패 층 분류(색인·탐색·생성)가 `REPORT.md`에 있다
- [ ] `README.md`에 실행 방법이 있고, 다른 컴퓨터에서 그대로 따라 하면 로컬에서 뜬다
- [ ] 화면 캡처가 들어가 있다
- [ ] API 키가 저장소에 없다

**기한:** 따로 정하지 않음.

---

## 9. 이번에 안 하는 것

- **공개 배포** — 로컬 실행까지만
- **로그인·회원가입·여러 사용자** — 혼자 쓰므로 불필요
- **커뮤니티 요약(전역 검색)** — 과제에서 명시적으로 제외
- **한국어 번역본 사용** — 번역가 저작권이 살아 있어 저장소에 못 올림
- **다른 책 지원** — 카라마조프 하나만
- **관계도 그림 그리기** — "나중에"
- **모바일 화면 대응**

---

## 10. 미정 사항

| 항목 | 언제 정해야 하나 |
|---|---|
| 몇 홉까지 확장할지 (기본 2홉 → 못 찾으면 3홉?) | 그래프를 만들고 실제 밀도를 본 뒤 |
| 허브 노드 처리 — 표도르, 알료샤처럼 거의 모든 장에 나오는 인물을 경유시킬지 | 노드 차수(연결 개수)를 세어본 뒤 |
| 노드·관계 스키마 확정 (인물/장소/사건/물건 · 가족/연인/원한/돈/사제 등) | `config.json` 만들 때 |
| 모델 사전지식 누수를 어떻게 막을지 — 프롬프트만으로 충분한지, 답변 검증 단계를 따로 둘지 | 1차 동작 후 실제로 새는지 시험해 보고 |
| 스포 차단이 "너무 많이 막는" 쪽으로 기울 때 허용 범위 | 사용자가 써보고 판단 |

---

## 참고 — 알려진 위험 5가지

1. **모델 사전 지식 누수 (가장 큼)** — 근거를 걷어내도 GPT가 자기 기억으로 결말을 말할 수 있음
2. **"이 관계가 몇 장에서 처음 드러났나" 판정 오류** — 틀리면 한 장 일찍 새거나 과하게 막힘
3. **별명 병합 자체가 스포** — 부칭을 합쳐 보여주는 것만으로 혈연 암시가 됨
4. **장 제목이 스포** — 안 읽은 장은 제목을 감추고 번호만 노출해 해결
5. **본문 쪼개기 실패** — 구텐베르크 원문에서 96장을 정확히 잘라내야 함. 장 수 확인 단계를 넣어 해결
